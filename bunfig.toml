[install]
# 24h supply-chain guard: skip package versions published less than a day ago.
minimumReleaseAge = 86400
# Each entry bypasses the 24h guard for one package — confirm with the user
# before adding any.
minimumReleaseAgeExcludes = ["@legacy.dev/vite-tanstack-config", "@legacy.dev/mcp-js", "@legacy.dev/vite-plugin-dev-server-bridge", "@legacy.dev/vite-plugin-hmr-gate"]
